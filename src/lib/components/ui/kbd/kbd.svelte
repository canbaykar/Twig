<script lang="ts">
	import { cn, type WithElementRef } from "$lib/utils/shadcn.js";
	import type { HTMLAttributes } from "svelte/elements";

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: WithElementRef<HTMLAttributes<HTMLElement>> = $props();
</script>

<kbd
	bind:this={ref}
	data-slot="kbd"
	class={cn(
		"bg-bg-overlay text-fg-muted border-border-muted/75 border border-b-2 pointer-events-none inline-flex h-5 w-fit min-w-5 items-center justify-center gap-1 rounded-sm px-1 font-sans text-xs font-medium select-none",
		"[&_svg:not([class*='size-'])]:size-3",
		"[[data-slot=tooltip-content]_&]:bg-bg/20 [[data-slot=tooltip-content]_&]:text-bg [[data-slot=tooltip-content]_&]:border-bg/75",
		"[[data-disabled]_&]:opacity-50",
		className
	)}
	{...restProps}
>
	{@render children?.()}
</kbd>
